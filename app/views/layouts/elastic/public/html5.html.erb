<!DOCTYPE html>
<html>
	<head>
		<meta charset="utf-8">		
		<meta http-equiv="Content-Type" content="application/xhtml+xml; charset=UTF-8" />
	
		<!-- Elastic-CMS html5 rails LAYOUT -->
		<meta name="keywords" content="<%= (@node and !@node.meta_keywords.blank?) ? @node.meta_keywords : Elastic::Context.site.meta_keywords %>" />
		<meta name="description" content="<%= (@node and !@node.meta_description.blank?) ? @node.meta_description : Elastic::Context.site.meta_description %>" />
	
		<link type="text/css" rel="Stylesheet" media="all" href="/x/current_theme/<%= @site.theme %>.css"/>		
		
		<%=raw @head %>
	</head>

	<body>		
		<%= yield %>		
		<%= render :partial=>'elastic/public/controls' %>
	</body>
	
</html>